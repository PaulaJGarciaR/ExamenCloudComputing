name: Destroy Infrastructure

on:
  workflow_dispatch:

jobs:
  Destroy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repositorio
        uses: actions/checkout@v3

      - name: Configurar credenciales AWS
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      # ─────────────────────────────────────────────
      # STACK 2 - EC2 ESTÁTICA (primero en destruir)
      # ─────────────────────────────────────────────
      - name: Eliminar Stack 2 - EC2 Estática
        run: |
          aws cloudformation delete-stack \
            --stack-name EC2-Paula-Darwin

      - name: Esperar eliminación Stack 2 - EC2 Estática
        run: |
          aws cloudformation wait stack-delete-complete \
            --stack-name EC2-Paula-Darwin
          echo "Stack 2 eliminado correctamente"

      # ─────────────────────────────────────────────
      # STACK 1 - SECURITY GROUPS (último en destruir)
      # ─────────────────────────────────────────────
      - name: Eliminar Stack 1 - Security Groups
        run: |
          aws cloudformation delete-stack \
            --stack-name SecurityGroups-Paula-Darwin

      - name: Esperar eliminación Stack 1 - Security Groups
        run: |
          aws cloudformation wait stack-delete-complete \
            --stack-name SecurityGroups-Paula-Darwin
          echo "Stack 1 eliminado correctamente"

      # ─────────────────────────────────────────────
      # STACK 4 - AUTOMATIZACIÓN (primero)
      # ─────────────────────────────────────────────
      - name: Eliminar Stack 4 - Automatización
        run: |
          aws cloudformation delete-stack \
            --stack-name Automation-Paula-Darwin

      - name: Esperar eliminación Stack 4
        run: |
          aws cloudformation wait stack-delete-complete \
            --stack-name Automation-Paula-Darwin
          echo "Stack 4 eliminado correctamente"

      # ─────────────────────────────────────────────
      # STACK 3 - ALB, ASG Y WARM POOL
      # ─────────────────────────────────────────────
      - name: Eliminar Stack 3 - ALB, ASG y Warm Pool
        run: |
          aws cloudformation delete-stack \
            --stack-name ALB-ASG-Paula-Darwin

      - name: Esperar eliminación Stack 3
        run: |
          aws cloudformation wait stack-delete-complete \
            --stack-name ALB-ASG-Paula-Darwin
          echo "Stack 3 eliminado correctamente"

